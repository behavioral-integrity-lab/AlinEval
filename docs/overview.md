Kanon is a behavioral stability framework that measures whether deployed AI models keep their promises when users pressure them to break it. By evaluating models' stability under pressure, Kanon can predict a ballpark of how safe the model is to be deployed in the real world, where there are consequences.

